<div data-role="page" id="orders_history">
    <div class="body">
    	<div class="orders_history_container">
    		<h4 class="history-title">Order history</h4>
    		<table>
    			<thead>
    				<tr>
    					<th>#</th>
    					<th>Total</th>
              <th>Business</th>
    					<th>Date</th>
              <th>ReOrder</th>
    				</tr>
    			</thead>
    			<tbody>
    				<% _.each(ordersHistory, function(order) { %>
    					<tr class="order-row" data-uuid="<%= order.orderUUID %>">
	    					<td><%= order.orderId %></td>
	    					<td>$<%= order.totalAmount.toFixed(2) %></td>
                <td><%= order.saslName %></td>
	    					<td><%= order.dateTimeOrderPlacedOn %></td>
                <td><a class="reorderButton" href="<%= order.reorderURL %>">ReOrder</a></td>
	    				</tr>
    				<% }) %>
    			</tbody>
    		</table>
    	</div>
    </div>
</div>
